{
  "name": "@workspace/web",
  "version": "0.0.0",
  "private": false,
  "type": "module",
  "scripts": {
    "clean": "git clean -fdx .cache .react-router .turbo build node_modules"
  },
  "dependencies": {
    "@workspace/api": "workspace:*"
  }
}
